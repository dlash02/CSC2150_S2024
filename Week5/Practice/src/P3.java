import java.util.ArrayList;
import java.util.Arrays;

public class P3 {
    public static void main(String[] args) {
        // Suppose you have the following 3 arrayLists
        ArrayList<String> menu = new ArrayList<String>(
                Arrays.asList("Pizza", "Hamburger", "Steak", "Pie"));
        ArrayList<Double> prices = new ArrayList<Double>(
                Arrays.asList(9.99, 10.99, 13.99, 5.99));
        ArrayList<String> order = new ArrayList<>(
                Arrays.asList("Pizza", "Pie")
        );
        // Suppose menu -> all items on menu
        //         prices -> the corresponding price of each item
        //               (E.g., Pizza -> 9.99 Pie -> 5.99
        //         order -> the items the customer ordered
        //  ToDo: Write the following method that returns
        //         the total bill for the items in order
//        Double bill = calculateBill( menu, prices, order);
        // Write another line that outputs all the items ordered
        //     and the total bill
        //  showTheOrder( order, bill);

    }


}
